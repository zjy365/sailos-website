---
quick_id: 260617-kt3
status: planned
created: 2026-06-17
---

# Quick Task 260617-kt3: Fix GHCR Docker image build failure

## Goal

Fix the GHCR Docker image workflow failure and add a guard for the static export
route contract that appeared in the latest failed Docker build log.

## Evidence

- The latest `Docker Build & Publish` run for `labring/sealos.io` failed in
  Docker Buildx while running `RUN npm ci && npm run build`.
- The failure text was:
  `Page "/[lang]/ai-quick-reference/[slug]" is missing "generateStaticParams()"`
  under `output: export`.
- A local Node 20 `npm run build` completed successfully and generated
  `/[lang]/ai-quick-reference/[slug]` paths, including
  `/en/ai-quick-reference/1-how-do-i-set-up`.

## Tasks

1. Repair the GHCR workflow refresh setup.
   - Files: `.github/workflows/build-image.yml`
   - Action: Install locked dependencies before running
     `npm run app-store:refresh`, and enable npm cache for the Node setup step.
   - Verify: Run deployment parity checks.

2. Keep deployment parity evidence aligned.
   - Files: `scripts/check-deployment-parity.js`,
     `scripts/check-deployment-parity.test.mjs`,
     `docs/deployment-parity.md`
   - Action: Require and document `npm ci` before the GHCR refresh step.
   - Verify: Run deployment parity unit tests and `npm run deployment:check`.

3. Add a regression guard for the Docker build log failure.
   - Files: `scripts/check-static-export-routes.test.mjs`
   - Action: Assert that the AI quick reference detail route exports
     `generateStaticParams()` from the FAQ corpus.
   - Verify: Run static export route tests and `npm run static-routes:check`.
